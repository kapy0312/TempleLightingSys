This file is a merged representation of a subset of the codebase, containing specifically included files, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of a subset of the repository's contents that is considered the most important context.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Only files matching these patterns are included: main.py, app.py, templates/index.html, static/js/app.js, requirements.txt, README.md
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
app.py
main.py
README.md
requirements.txt
static/js/app.js
templates/index.html
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="static/js/app.js">
let isAdmin = false;
const bootstrapModals = {
    login: new bootstrap.Modal(document.getElementById("loginModal")),
    record: new bootstrap.Modal(document.getElementById("recordModal")),
    edit: new bootstrap.Modal(document.getElementById("editModal"))
};
document.addEventListener("DOMContentLoaded", () => {
    checkAuthStatus();
});

async function checkAuthStatus() {
    const res = await fetch("/api/auth/status");
    const data = await res.json();
    isAdmin = data.logged_in;

    document.getElementById("btnLogin").classList.toggle("d-none", isAdmin);
    document.getElementById("btnLogout").classList.toggle("d-none", !isAdmin);
    document.getElementById("btnAdd").classList.toggle("d-none", !isAdmin);

    document
        .querySelectorAll(".admin-col")
        .forEach((el) => el.classList.toggle("d-none", !isAdmin));
    loadRecords();
}

document.getElementById("submitLogin").addEventListener("click", async () => {
    const res = await fetch("/api/auth/login", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
            username: document.getElementById("username").value,
            password: document.getElementById("password").value,
        }),
    });

    if (res.ok) {
        bootstrapModals.login.hide();
        checkAuthStatus();
    } else {
        alert("登入失敗");
    }
});

document.getElementById("btnLogout").addEventListener("click", async () => {
    await fetch("/api/auth/logout", { method: "POST" });
    checkAuthStatus();
});

// [NEW] 完整實作渲染邏輯，並確保支援管理員權限切換
async function loadRecords(query = "", forceAll = false) {
    let url = `/api/records?q=${encodeURIComponent(query)}`;
    if (forceAll) url += "&all=true";

    const res = await fetch(url);
    const records = await res.json();
    const tbody = document.getElementById("recordTableBody");
    tbody.innerHTML = "";

    records.forEach((r) => {
        const tr = document.createElement("tr");
        const nameArray = r.names.split(/[,，\s]+/);
        const namesContainer = document.createElement("div");
        namesContainer.className = "names-cell";

        nameArray.forEach((name) => {
            if (name.trim()) {
                const div = document.createElement("div");
                div.textContent = name.trim();
                namesContainer.appendChild(div);
            }
        });

        // 構建操作按鈕 (僅在管理員模式顯示)
        const safeData = JSON.stringify(r).replace(/'/g, "&apos;");
        const adminBtns = isAdmin ? `
        <td class="text-center admin-col">
            <button class="btn btn-warning btn-sm me-1" onclick='openEditModal(${safeData})'>
                <i class="bi bi-pencil-square"></i> 編輯
            </button>
            <button class="btn btn-danger btn-sm" onclick="deleteRecord(${r.id})">
                <i class="bi bi-trash"></i> 刪除
            </button>
        </td>` : '<td class="admin-col d-none"></td>';

        tr.innerHTML = `
            <td>${r.id}</td>
            <td id="names-${r.id}"></td>
            <td>$${r.amount}</td>
            <td>${r.altar_name}</td>
            ${adminBtns}
        `;

        tbody.appendChild(tr);
        document.getElementById(`names-${r.id}`).appendChild(namesContainer);
    });
}

// [MODIFIED] 修正新增紀錄後的刷新邏輯 (約第 125 行)
document.getElementById("saveRecord").addEventListener("click", async () => {
    const payload = {
        names: document.getElementById("names").value,
        amount: parseInt(document.getElementById("amount").value),
        altar_name: document.getElementById("altarName").value,
    };

    await fetch("/api/records", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify(payload),
    });

    bootstrapModals.record.hide();

    // [NEW] 新增後自動顯示全部，確保使用者看到剛才新增的資料
    loadRecords('', true);

    document.getElementById("names").value = "";
    document.getElementById("amount").value = "";
    document.getElementById("altarName").value = "";
});

// [MODIFIED] 修正刪除紀錄後的刷新邏輯 (約第 142 行)
window.deleteRecord = async (id) => {
    if (!confirm("確認刪除此紀錄？")) return;
    await fetch(`/api/records/${id}`, { method: "DELETE" });

    // [NEW] 刪除後依照目前的搜尋框內容重新整理
    const q = document.getElementById('searchInput').value;
    loadRecords(q, q === '');
};

// [INSERT] 修正按鈕事件
document.getElementById('btnSearch').addEventListener('click', () => {
    loadRecords(document.getElementById('searchInput').value, false);
});

document.getElementById('btnShowAll').addEventListener('click', () => {
    document.getElementById('searchInput').value = '';
    loadRecords('', true); // [MODIFIED] 傳入 true 觸發後端 show_all
});

// [INSERT] 補全缺失的編輯視窗邏輯
window.openEditModal = (data) => {
    document.getElementById('editId').value = data.id;
    document.getElementById('editNames').value = data.names;
    document.getElementById('editAmount').value = data.amount;
    document.getElementById('editAltarName').value = data.altar_name;
    bootstrapModals.edit.show();
};

document.getElementById('updateRecord').addEventListener('click', async () => {
    const id = document.getElementById('editId').value;
    const payload = {
        names: document.getElementById('editNames').value,
        amount: parseInt(document.getElementById('editAmount').value),
        altar_name: document.getElementById('editAltarName').value
    };
    const res = await fetch(`/api/records/${id}`, {
        method: 'PUT',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(payload)
    });
    if (res.ok) {
        bootstrapModals.edit.hide();
        loadRecords(document.getElementById('searchInput').value, !document.getElementById('searchInput').value);
    }
});

// [INSERT] 於檔案末端
document.getElementById('btnExport').addEventListener('click', async () => {
    // 強制獲取完整資料庫紀錄 [cite: 5]
    const res = await fetch('/api/records?all=true');
    const records = await res.json();

    if (records.length === 0) {
        alert('無資料可匯出');
        return;
    }

    // 定義 CSV 標頭
    const headers = ['編號', '姓名', '金額', '壇名'];

    // 處理資料行：將姓名中的換行、逗號與空白替換，避免破壞 CSV 結構
    const rows = records.map(r => [
        r.id,
        r.names.replace(/[\n\r,，\s]+/g, ' '),
        r.amount,
        r.altar_name
    ]);

    // 組合 CSV 內容：
    // 使用 "\uFEFF" (UTF-8 BOM) 是關鍵，確保 Excel 在繁體中文環境下開啟不亂碼
    const csvContent = "\uFEFF" + [headers, ...rows].map(e => e.join(",")).join("\n");

    // 建立 Blob 與下載連結
    const blob = new Blob([csvContent], { type: 'text/csv;charset=utf-8;' });
    const link = document.createElement("a");
    const url = URL.createObjectURL(blob);
    const dateStr = new Date().toISOString().split('T')[0];

    link.setAttribute("href", url);
    link.setAttribute("download", `點燈清單匯出_${dateStr}.csv`);
    link.style.visibility = 'hidden';
    document.body.appendChild(link);
    link.click();
    document.body.removeChild(link);
});

// 觸發隱藏的檔案選擇器
document.getElementById('btnImport').addEventListener('click', () => {
    document.getElementById('importFile').click();
});

// 當使用者選好檔案後觸發
document.getElementById('importFile').addEventListener('change', async (e) => {
    const file = e.target.files[0];
    if (!file) return;

    // 二次確認視窗
    const msg = "⚠️ 【危險操作警告】\n\n這將會「刪除目前資料庫中的所有紀錄」，並以匯入的 CSV 內容完全取代。\n\n確定要繼續嗎？此操作無法復原。";
    if (!confirm(msg)) {
        e.target.value = ''; // 取消則清空選擇
        return;
    }

    const formData = new FormData();
    formData.append('file', file);

    try {
        const res = await fetch('/api/records/import', {
            method: 'POST',
            body: formData
        });

        const result = await res.json();
        if (res.ok) {
            alert("✅ 資料匯入成功！");
            loadRecords('', true); // 重新讀取清單並顯示全部
        } else {
            alert("❌ " + result.error);
        }
    } catch (err) {
        alert("❌ 傳輸過程發生錯誤");
    } finally {
        e.target.value = ''; // 執行完後清空
    }
});
</file>

<file path="app.py">
from io import BytesIO  # 新增
import openpyxl  # 新增
import os
import sys
import csv
import openpyxl  # 處理 Excel 的核心
from io import BytesIO, TextIOWrapper  # <--- 把這兩個寫在一起
from flask import Flask, jsonify, request, session, render_template
from flask_sqlalchemy import SQLAlchemy
from werkzeug.security import generate_password_hash, check_password_hash

if getattr(sys, 'frozen', False):
    base_dir = sys._MEIPASS
    db_dir = os.path.dirname(sys.executable)
else:
    base_dir = os.path.abspath(os.path.dirname(__file__))
    db_dir = base_dir

app = Flask(
    __name__,
    template_folder=os.path.join(base_dir, 'templates'),
    static_folder=os.path.join(base_dir, 'static')
)

app.secret_key = 'temple_secret_key_v1'
app.config['SQLALCHEMY_DATABASE_URI'] = f"sqlite:///{os.path.join(db_dir, 'temple_data.db')}"
app.config['SQLALCHEMY_TRACK_MODIFICATIONS'] = False

db = SQLAlchemy(app)


class User(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    username = db.Column(db.String(50), unique=True, nullable=False)
    password_hash = db.Column(db.String(255), nullable=False)


class LightRecord(db.Model):
    id = db.Column(db.Integer, primary_key=True, autoincrement=True)
    names = db.Column(db.Text, nullable=False)
    amount = db.Column(db.Integer, nullable=False)
    altar_name = db.Column(db.Text, nullable=False)


def init_db():
    with app.app_context():
        db.create_all()
        if not User.query.filter_by(username='admin').first():
            hashed_pw = generate_password_hash('admin123')
            admin_user = User(username='admin', password_hash=hashed_pw)
            db.session.add(admin_user)
            db.session.commit()


init_db()


def is_admin():
    return session.get('logged_in', False)


@app.route('/')
def index():
    return render_template('index.html')


@app.route('/api/auth/status', methods=['GET'])
def auth_status():
    return jsonify({"logged_in": is_admin()})


@app.route('/api/auth/login', methods=['POST'])
def login():
    data = request.json
    user = User.query.filter_by(username=data.get('username')).first()
    if user and check_password_hash(user.password_hash, data.get('password')):
        session['logged_in'] = True
        return jsonify({"success": True})
    return jsonify({"success": False, "message": "帳號或密碼錯誤"}), 401


@app.route('/api/auth/logout', methods=['POST'])
def logout():
    session.pop('logged_in', None)
    return jsonify({"success": True})


# [NEW] 合併後的正確邏輯：支援「空搜尋」、「特定搜尋」與「強制全部」
@app.route('/api/records', methods=['GET'])
def get_records():
    search = request.args.get('q', '')
    show_all = request.args.get('all', 'false') == 'true'

    if show_all:
        # 強制顯示全部
        records = LightRecord.query.all()
    elif search:
        # 模糊搜尋
        records = LightRecord.query.filter(
            (LightRecord.names.contains(search)) |
            (LightRecord.altar_name.contains(search))
        ).all()
    else:
        # 初始狀態或空搜尋，回傳空清單（符合 User 需求）
        records = []

    result = [{
        "id": r.id, "names": r.names,
        "amount": r.amount, "altar_name": r.altar_name
    } for r in records]
    return jsonify(result)

# [INSERT] 新增更新 API at line 95


@app.route('/api/records', methods=['POST'])
def add_record():
    if not is_admin():
        return jsonify({"error": "Unauthorized"}), 403
    data = request.json
    new_record = LightRecord(
        names=data.get('names'),
        amount=data.get('amount'),
        altar_name=data.get('altar_name')
    )
    db.session.add(new_record)
    db.session.commit()
    return jsonify({"success": True, "id": new_record.id})


@app.route('/api/records/<int:record_id>', methods=['PUT'])
def update_record(record_id):
    if not is_admin():
        return jsonify({"error": "Unauthorized"}), 403
    data = request.json
    record = LightRecord.query.get_or_404(record_id)
    record.names = data.get('names', record.names)
    record.amount = data.get('amount', record.amount)
    record.altar_name = data.get('altar_name', record.altar_name)
    db.session.commit()
    return jsonify({"success": True})


@app.route('/api/records/<int:record_id>', methods=['DELETE'])
def delete_record(record_id):
    if not is_admin():
        return jsonify({"error": "Unauthorized"}), 403
    record = LightRecord.query.get_or_404(record_id)
    db.session.delete(record)
    db.session.commit()
    return jsonify({"success": True})


@app.route('/api/records/import', methods=['POST'])
def import_records():
    if not is_admin():
        return jsonify({"error": "Unauthorized"}), 403

    file = request.files.get('file')
    if not file:
        return jsonify({"error": "未選取檔案"}), 400

    filename = file.filename.lower()
    all_new_records = []

    try:
        # --- 處理 Excel (.xlsx) ---
        if filename.endswith('.xlsx'):
            wb = openpyxl.load_workbook(BytesIO(file.read()), data_only=True)
            for sheet in wb.worksheets:
                rows = list(sheet.rows)
                if not rows:
                    continue

                header_row = [str(cell.value).strip()
                              if cell.value else "" for cell in rows[0]]

                idx_id = header_row.index("編號") if "編號" in header_row else -1
                idx_name = -1
                for n in ["姓名", "信眾姓名"]:
                    if n in header_row:
                        idx_name = header_row.index(n)
                        break
                idx_amount = header_row.index(
                    "金額") if "金額" in header_row else -1
                idx_altar = header_row.index(
                    "壇名") if "壇名" in header_row else -1

                if -1 in [idx_id, idx_name, idx_amount, idx_altar]:
                    continue

                current_main = None
                for row_data in rows[1:]:
                    raw_id = str(row_data[idx_id].value).strip(
                    ) if row_data[idx_id].value is not None else ""
                    name = str(row_data[idx_name].value).strip(
                    ) if row_data[idx_name].value is not None else ""
                    altar = str(row_data[idx_altar].value).strip(
                    ) if row_data[idx_altar].value is not None else ""

                    amt_val = str(row_data[idx_amount].value).replace(
                        ',', '') if row_data[idx_amount].value is not None else "0"
                    amount = int(float(amt_val)) if amt_val.replace(
                        '.', '').isdigit() else 0

                    if not raw_id or not name:
                        continue

                    is_sub = "." in raw_id and not raw_id.endswith(".0")

                    if not is_sub:
                        if current_main:
                            all_new_records.append(current_main)
                        current_main = {
                            "names": [name], "amount": amount, "altar": altar}
                    else:
                        if current_main:
                            current_main["names"].append(name)

                if current_main:
                    all_new_records.append(current_main)

        # --- 處理 CSV (.csv) ---
        elif filename.endswith('.csv'):
            file_content = file.read().decode('utf-8-sig')  # 處理 BOM 問題
            reader = csv.reader(file_content.splitlines())

            # 讀取標頭並清理空白
            header = [h.strip() for h in next(reader)]

            idx_id = header.index("編號") if "編號" in header else -1
            idx_name = -1
            for n in ["姓名", "信眾姓名"]:
                if n in header:
                    idx_name = header.index(n)
                    break
            idx_amount = header.index("金額") if "金額" in header else -1
            idx_altar = header.index("壇名") if "壇名" in header else -1

            if -1 not in [idx_id, idx_name, idx_amount, idx_altar]:
                current_main = None
                for row_data in reader:
                    # 如果該行為空或欄位不足，則跳過
                    if len(row_data) < max(idx_id, idx_name, idx_amount, idx_altar) + 1:
                        continue

                    raw_id = str(row_data[idx_id]).strip()
                    name = str(row_data[idx_name]).strip()
                    altar = str(row_data[idx_altar]).strip()

                    amt_val = str(row_data[idx_amount]).replace(',', '')
                    amount = int(float(amt_val)) if amt_val.replace(
                        '.', '').isdigit() else 0

                    if not raw_id or not name:
                        continue

                    # 子編號合併邏輯
                    is_sub = "." in raw_id and not raw_id.endswith(".0")

                    if not is_sub:
                        if current_main:
                            all_new_records.append(current_main)
                        # 如果是匯出的 CSV，姓名可能已經是用空格隔開的多人，這裡直接當作單一名稱存入（後續寫入資料庫會再處理）
                        current_main = {
                            "names": [name], "amount": amount, "altar": altar}
                    else:
                        if current_main:
                            current_main["names"].append(name)

                if current_main:
                    all_new_records.append(current_main)

        # --- 統一寫入資料庫 ---
        if all_new_records:
            LightRecord.query.delete()  # 覆蓋模式
            for r in all_new_records:
                # 把陣列裡的姓名用分號合併，如果原本裡面就有空格也會保留
                final_names = ",".join(r["names"])
                # 這裡再將系統匯出時使用的空格，轉換為內部儲存用的分號，確保跨系統一致性
                final_names = final_names.replace(" ", ",")

                db.session.add(LightRecord(
                    names=final_names,
                    amount=r["amount"],
                    altar_name=r["altar"]
                ))
            db.session.commit()
            return jsonify({"success": True, "count": len(all_new_records)})
        else:
            return jsonify({"error": "找不到符合格式的資料，請檢查標頭名稱是否包含：編號、姓名、金額、壇名"}), 400

    except Exception as e:
        db.session.rollback()
        return jsonify({"error": f"匯入崩潰: {str(e)}"}), 500
</file>

<file path="README.md">
# 正坤點燈管理系統 (Temple Lighting Management System)

> 專為宮廟設計的輕量化點燈資料管理系統，以 Python Flask 驅動後端，並透過 `pywebview` 封裝為免安裝的原生桌面應用程式。

---

## ✨ 功能特色

| 功能 | 說明 |
|------|------|
| **角色權限控制 (RBAC)** | 訪客模式僅供查詢；管理員登入後開放完整 CRUD 操作 |
| **多姓名渲染** | 姓名欄支援逗號、空白分隔，前端自動轉為垂直排列顯示 |
| **智慧匯入** | 支援 `.xlsx` / `.csv`，自動識別標頭、合併子編號（如 4.1, 4.2） |
| **CSV 匯出** | 含 UTF-8 BOM，確保 Excel 在繁體中文環境下開啟不亂碼 |
| **零配置部署** | 自動初始化 SQLite 資料庫，隨插即用，無需額外設定 |
| **關鍵字搜尋** | 支援依姓名或壇名進行模糊搜尋 |

---

## 🛠️ 技術棧

| 領域 | 技術 |
|------|------|
| **後端** | Python 3.12.10, Flask 3.0.2, Flask-SQLAlchemy 3.1.1, openpyxl 3.1.2 |
| **前端** | Vanilla JavaScript, Bootstrap 5, Bootstrap Icons |
| **資料庫** | SQLite（檔案式，零配置） |
| **桌面化** | pywebview 4.4.1（Edge Chromium 核心） |
| **打包工具** | PyInstaller 6.4.0 |

---

## 📁 專案結構

```
TempleLightingSys/
├── main.py                  # 桌面應用程式啟動入口（執行緒管理）
├── app.py                   # Flask RESTful API、資料庫模型、檔案解析邏輯
├── templates/
│   └── index.html           # 前端 UI（Bootstrap 5 + Glassmorphism 設計）
├── static/
│   ├── background.png       # 系統背景圖
│   ├── css/
│   │   ├── bootstrap.min.css
│   │   └── bootstrap-icons.min.css
│   └── js/
│       ├── app.js           # 前端業務邏輯與 API 互動
│       └── bootstrap.bundle.min.js
├── favicon.ico              # 應用程式圖示
├── requirements.txt         # 相依套件清單
└── temple_data.db           # SQLite 資料庫（執行後自動產生）
```

---

## 🚀 快速開始

> ⚠️ **執行以下指令前，請確保終端機顯示 `(venv)`，且位於專案根目錄。**

### 1. 環境需求

- **Python 3.12.10**（必須，確保 `pythonnet` 與套件相容性，勿使用過新版本）
- Windows 作業系統（pywebview 使用 Edge Chromium 核心）

### 2. 建立虛擬環境

```powershell
python -m venv venv
.\venv\Scripts\activate
```

### 3. 安裝相依套件

```powershell
pip install -r requirements.txt
```

### 4. 啟動應用程式

```powershell
python main.py
```

應用程式視窗將自動開啟，預設監聽 `http://127.0.0.1:5001`。

---

## 🔐 預設管理員帳號

| 欄位 | 值 |
|------|----|
| 帳號 | `admin` |
| 密碼 | `admin123` |

> ⚠️ 建議於正式部署前修改預設密碼。

---

## 📊 資料匯入格式

匯入的 Excel / CSV 檔案需包含以下標頭欄位：

| 標頭名稱 | 說明 |
|----------|------|
| `編號` | 流水號，子編號格式如 `1.1`、`1.2` 將自動合併至主紀錄 |
| `姓名` 或 `信眾姓名` | 點燈者姓名 |
| `金額` | 點燈金額（數字） |
| `壇名` | 所屬壇號 |

> 匯入為**覆蓋模式**，執行後將清除現有資料庫中所有紀錄。

---

## 📦 打包為執行檔 (.exe)

```powershell
pyinstaller -F -w --clean \
  --name "正坤點燈管理系統" \
  --icon="favicon.ico" \
  --add-data "templates;templates" \
  --add-data "static;static" \
  main.py
```

打包完成後，執行檔位於 `dist/` 資料夾。

**交付說明：**
- 僅需提供 `正坤點燈管理系統.exe` 給使用者。
- 若需保留既有資料，請將 `temple_data.db` 放置於與 `.exe` 相同的目錄下。

---

## 🔧 開發常用指令

```powershell
# 查看已安裝套件
pip list

# 更新 requirements.txt
pip freeze > requirements.txt
```

---

## 📋 API 端點一覽

| 方法 | 路徑 | 權限 | 說明 |
|------|------|------|------|
| `GET` | `/api/auth/status` | 公開 | 取得登入狀態 |
| `POST` | `/api/auth/login` | 公開 | 管理員登入 |
| `POST` | `/api/auth/logout` | 公開 | 登出 |
| `GET` | `/api/records` | 公開 | 查詢紀錄（支援 `?q=` 搜尋、`?all=true` 顯示全部） |
| `POST` | `/api/records` | 管理員 | 新增紀錄 |
| `PUT` | `/api/records/<id>` | 管理員 | 更新紀錄 |
| `DELETE` | `/api/records/<id>` | 管理員 | 刪除紀錄 |
| `POST` | `/api/records/import` | 管理員 | 匯入 CSV / XLSX |

---

## 📄 授權

本專案為內部使用系統，僅供正坤宮廟管理用途。
</file>

<file path="requirements.txt">
altgraph==0.17.5
blinker==1.9.0
bottle==0.13.4
cffi==2.0.0
click==8.3.3
clr_loader==0.2.10
colorama==0.4.6
Flask==3.0.2
Flask-SQLAlchemy==3.1.1
greenlet==3.4.0
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
packaging==26.1
pefile==2024.8.26
proxy_tools==0.1.0
pycparser==3.0
pyinstaller==6.4.0
pyinstaller-hooks-contrib==2026.4
pythonnet==3.0.5
pywebview==4.4.1
pywin32-ctypes==0.2.3
setuptools==82.0.1
SQLAlchemy==2.0.49
typing_extensions==4.15.0
Werkzeug==3.0.1
openpyxl==3.1.2
</file>

<file path="main.py">
import webview
import threading
import time
from app import app


def start_flask():
    # 使用 threaded=True 確保 Flask 不會阻塞主執行緒
    app.run(port=5001, threaded=True)


if __name__ == '__main__':
    # 1. 先啟動 Flask
    t = threading.Thread(target=start_flask)
    t.daemon = True
    t.start()

    # 2. 稍微等待 1 秒確保伺服器已就緒
    time.sleep(1)

    # 3. 建立視窗，明確指定 localhost URL 而非傳入 app 物件
    window = webview.create_window(
        title='正坤點燈資料管理系統 v1.0.3',
        url='http://127.0.0.1:5001',
        width=1024,
        height=768,
        min_size=(800, 600),
        text_select=True  # <--- 新增這一行，允許文字選取與複製
    )

    # 4. 啟動視窗 (關閉 debug 以獲得乾淨介面)
    webview.start(gui='edgechromium')
</file>

<file path="templates/index.html">
<!doctype html>
<html lang="zh-TW">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>正坤點燈管理系統</title>
    <link href="/static/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/static/css/bootstrap-icons.min.css">

    <style>
        body {
            /* 背景圖設定 */
            background-image: url('/static/background.png');
            background-size: cover;
            background-attachment: fixed;
            background-position: center;
            background-repeat: no-repeat;
            /* 預備背景色，圖片沒跑出來時撐場用 */
            background-color: #f8f9fa;
        }

        /* 核心容器優化：讓它像一張漂浮的磨砂玻璃卡片 */
        .container {
            /* 90% 不透明白色背景 */
            background-color: rgba(255, 255, 255, 0.8);

            /* [進階視覺] 讓背景有一點點模糊效果，更有質感 (選配) */
            backdrop-filter: blur(5px);
            -webkit-backdrop-filter: blur(5px);

            padding: 30px;
            border-radius: 15px;
            margin-top: 30px;
            margin-bottom: 50px;

            /* 增加陰影，讓容器跟背景圖有層次感 */
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);

            /* 限制寬度不要太撐開，閱讀比較舒服 */
            max-width: 1100px;
        }

        /* 標題文字加深，確保對比度 */
        h2,
        h3 {
            color: #333;
            font-weight: 700;
            margin-bottom: 20px;
        }

        /* 表格內的姓名格線稍微加深，以免在白背景下看不清 */
        .names-cell div {
            border-bottom: 1px dashed #bbb;
            padding: 4px 0;
        }

        .names-cell div:last-child {
            border-bottom: none;
        }

        /* 讓表格標題背景深一點，增加穩定感 */
        .table-dark {
            --bs-table-bg: #343a40;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container-fluid">
            <span class="navbar-brand mb-0 h1">正坤點燈管理系統</span>
            <div>
                <button id="btnLogin" class="btn btn-outline-light btn-sm" data-bs-toggle="modal"
                    data-bs-target="#loginModal">
                    登入 (管理員)
                </button>
                <button id="btnLogout" class="btn btn-danger btn-sm d-none">
                    登出
                </button>
            </div>
        </div>
    </nav>

    <div class="container mt-4">

        <div class="row g-2 mb-3 bg-light p-3 rounded border">
            <div class="col-auto">
                <input type="text" id="searchInput" class="form-control" placeholder="輸入關鍵字搜尋...">
            </div>
            <div class="col-auto">
                <button id="btnSearch" class="btn btn-secondary">查詢</button>
                <button id="btnShowAll" class="btn btn-outline-secondary">顯示全部清單</button>
                <button id="btnExport" class="btn btn-success ms-1">
                    <i class="bi bi-file-earmark-spreadsheet"></i> 匯出 CSV
                </button>
                <button id="btnImport" class="btn btn-warning ms-1 admin-col d-none">
                    <i class="bi bi-file-earmark-arrow-up"></i> 匯入 CSV / XLSX
                </button>
                <input type="file" id="importFile"
                    accept=".csv, .xlsx, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
                    class="d-none">
            </div>
        </div>

        <div class="d-flex justify-content-between align-items-center mb-3">
            <h3>點燈清單</h3>
            <button id="btnAdd" class="btn btn-primary d-none" data-bs-toggle="modal" data-bs-target="#recordModal">
                新增紀錄
            </button>
        </div>
        <table class="table table-bordered table-hover bg-white shadow-sm">
            <thead class="table-light">
                <tr>
                    <th width="10%">ID</th>
                    <th width="35%">姓名</th>
                    <th width="15%">金額</th>
                    <th width="25%">壇名</th>
                    <th width="15%" class="text-center admin-col d-none">操作</th>
                </tr>
            </thead>
            <tbody id="recordTableBody"></tbody>
        </table>
    </div>

    <div class="modal fade" id="loginModal" tabindex="-1">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">管理員登入</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="text" id="username" class="form-control mb-2" placeholder="帳號" value="admin" />
                    <input type="password" id="password" class="form-control" placeholder="密碼" />
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary w-100" id="submitLogin">
                        登入
                    </button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="recordModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">新增點燈紀錄</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">姓名
                            <small class="text-muted">(多筆以空白或逗號分隔)</small></label>
                        <input type="text" id="names" class="form-control" placeholder="例如: 吳雅惠, 李毓卿" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">金額</label>
                        <input type="number" id="amount" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">壇名</label>
                        <input type="text" id="altarName" class="form-control" />
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" id="saveRecord">
                        儲存
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- // [INSERT] at line 122 (recordModal 結束標籤後) -->
    <div class="modal fade" id="editModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">編輯點燈紀錄</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <input type="hidden" id="editId">
                    <div class="mb-3">
                        <label class="form-label">姓名</label>
                        <input type="text" id="editNames" class="form-control">
                    </div>
                    <div class="mb-3">
                        <label class="form-label">金額</label>
                        <input type="number" id="editAmount" class="form-control">
                    </div>
                    <div class="mb-3">
                        <label class="form-label">壇名</label>
                        <input type="text" id="editAltarName" class="form-control">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-success" id="updateRecord">更新</button>
                </div>
            </div>
        </div>
    </div>

    <script src="/static/js/bootstrap.bundle.min.js"></script>
    <script src="/static/js/app.js"></script>
</body>

</html>
</file>

</files>
